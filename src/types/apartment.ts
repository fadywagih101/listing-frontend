export interface Apartment {
  id: number;
  name: string;
  unitNumber: string;
  project: string;
  description?: string;
  imageUrl?: string;
  space: string;
  price: string;
  address: string;
}
